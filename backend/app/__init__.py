"""Metal Detector Studio — bench lab backend.

Universal, profile-driven telemetry suite for custom metal detectors (VLF / PI).
The active device is described by a JSON profile (see ``backend/profiles/*.json``);
nothing here is hardcoded to a single detector.
"""

__version__ = "0.1.0"
